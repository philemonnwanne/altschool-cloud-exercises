server {
       listen 80;
       listen [::]:80;

       server_name {{ http_host }};

       root {{ document_root }};
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
